/* Arrays */
let days = ['mod','tue','wed','thur','fri','sat','sun']
// days.unshift('monday'); // poveke vrednosti prima
// days.pop(days.length-1);
// days.push('king') // poveke vrednosti prima
// days.shift();
// console.log(days)
days.splice(1,2); // start position / delete count
console.log(days)